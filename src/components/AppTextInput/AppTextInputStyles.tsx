import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    width: '100%',
    backgroundColor: '#222124ff',
    color: '#ffffffff',
    fontSize:20,
    height: 60,
    padding: 16,
    borderRadius: 8,
    textAlign: 'center'
  }
});